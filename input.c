#include<stdio.h>
int main(){
    float radius,pi;
    // radius=5;
    printf("the radius is::");
    scanf("%f",&radius);
    pi=3.1415;
    printf("the area of circle is %f",pi*radius*radius);

    return 0;
}