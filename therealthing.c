#include<stdio.h>
int main(){
    int a;
    // if(expression)
    // statement
    if(3+2%5)
    printf("this works");
    // if(a=10)
    // printf("\nthissss");
    if(a==10)
    printf("\nhello world");
    if('a')
    printf("\nhello guyz");
    if(!0)
    printf("\ngamma");
    
    return 0;
}